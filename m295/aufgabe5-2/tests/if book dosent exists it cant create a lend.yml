info:
  name: if book dosent exists it cant create a lend
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/lends
  body:
    type: json
    data: |-
      {
        "customerId":"1",
        "isbn":"9780140328721"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("lend if book dosent exist", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
