info:
  name: Create book
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/books
  body:
    type: json
    data: |-
      {
        "isbn": "978-3-98-765432-1",
        "title": "Clean Code",
        "year": 2008,
        "author": "Robert C. Martin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
