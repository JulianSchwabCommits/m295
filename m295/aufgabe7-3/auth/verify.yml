info:
  name: verify
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/verify
  body:
    type: json
    data: '{"email":"admin@library.ch"}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
