info:
  name: Add name to list
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/names
  body:
    type: form-urlencoded
    data:
      - name: name
        value: Julian
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
